import {Box, styled} from "@mui/material";

export const StyledBoxFooter = styled(Box)(() => ({
    backgroundColor: "#616161",
    color: "white",
    padding: "10px 0",
    textAlign: "center",
    position: "absolute",
    bottom: "0",
    width: "100%",
    height: "2rem",
}));
